services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      HOST: 0.0.0.0
      JWT_SECRET: local-dev-secret-change-me
      # DATABASE_URL: postgresql://...  # set to use Neon
    volumes:
      - forgeledger-data:/app/data

volumes:
  forgeledger-data:
